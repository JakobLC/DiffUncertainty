# @package _global_
model:
  _target_: uncertainty_modeling.global_utils.model_factory.instantiate_network
  target: ${network.target}
  cfg: ${network.cfg}
  overrides:
    MODEL:
      PRETRAINED: true
      EXTRA:
        DROPOUT_FINAL: true
